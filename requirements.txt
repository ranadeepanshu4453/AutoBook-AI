# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
aiomysql==0.2.0
motor==3.6.0

# AI / ML
sentence-transformers==5.5.1
scikit-learn==1.5.2
numpy==1.26.4

# Utilities
python-dotenv==1.0.1
dateparser==1.2.0
pandas==2.2.3

# Vector
chromadb

# Token
pyjwt 
httpx

# Removed — not needed in production
# mysql-connector-python  (using aiomysql instead, don't need both)
# chromadb                (not used in current codebase)
# streamlit               (frontend only, separate deployment)
ollama                  #(commented out already)
# source venv/bin/activate
# ai model create folder and run cmd to install it 
#mkdir AI_models
# pip install sentence-transformers
# python -c "
# from sentence_transformers import SentenceTransformer
# model = SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2')
# model.save('AI_models/all-MiniLM-L6-v2')
# "
 